image: python:3.9

stages:
  - deploy

deploy:
  stage: deploy
  script:
    - pip install -r requirements.txt
    - npm install -g vercel
    - vercel --prod --token=$VERCEL_TOKEN
  only:
    - dev  # Deploy only when pushing to 'dev'
